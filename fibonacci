#include <bits/stdc++.h>
using namespace std;

//
int fibonacci(int n){
    int first = 0;
    int second = 1;
    if (n < 2){
        return n;
    }
    for (int i = 1; i < n; i++){
        second = first + second;
        first = second - first;
    }
    return second;
}
